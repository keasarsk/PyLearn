print('') print("") 							自带换行
type(变量名)										返回变量类型
print(" {}  {} ".format(变量名1,变量名2))			格式化输出 作用类似%转义
变量=input("提示语")   							返回获取键盘输入的内容 单引号也可

if 条件：										记得冒号：
	执行代码
	elif 条件：
		执行代码
	else：
		执行代码

import random 									导入产生随机数的模块
random.randint(0,2)								返回0<=  <=2的随机数 等价于randrange(0,3)

while 条件表达式:
    代码指令
语法需要
1.有初始值
2.条件表达式
3.变量【循环体内计数变量】的自增自减, 否则会造成死循环

for 临时变量 in 容器:								依次的取!!集合容器!!中的每个值 不像Java需要循环计数 
    执行代码块

break 											中断结束   满足条件直接的结束本层循环
continue										结束本次循环，进行下次循环（本次循环剩下的语句将不在执行
												这两个关键字只能用在循环中

range(1,20)										依次生成1-19

print(id(变量名))								获取物理（引用）地址

li=[1,2,3,"你好"]
print(len(li))									len(列表名)获得列表数据个数






















